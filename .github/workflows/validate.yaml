name: Test & Lint

on:
  push:
    branches:
      - "**"
    tags:
      - "v*.*.*"
  pull_request:
    branches:
      - "main"

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: extractions/setup-just@v1
      - name: Install JQ
        run: GH_TOKEN=${{GH_TOKEN}} just install-jq
      - name: Install GCloud
        run: just install-gcloud
